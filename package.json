{
  "name": "freelancer-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../server && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd frontend && npm start\"",
    "vercel-build": "cd frontend && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "concurrently": "^8.2.2",
    "dotenv": "^16.5.0",
    "stripe": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.3",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "typescript": "^5.8.3"
  }
}
